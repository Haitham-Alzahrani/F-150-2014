# Runtime dependencies for the diagnostic tool.
#
#   .venv/bin/pip install -r requirements.txt

pyserial>=3.5        # ELM327 adapter over a serial port
PyYAML>=6.0          # protocol and knowledge-base files
